import * as React from 'react';

const VideoConference = (props) => {
    return {
        <div>
            <h2>{props.name}</h2>
            <p>{props.userAdmin}</p>
        </div>
    }
}

export default VideoConference;